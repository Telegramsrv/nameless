"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const button_holding_builder_1 = require("./button-holding-builder");
class OgElementBuilder extends button_holding_builder_1.ButtonHoldingBuilder {
    constructor(url) {
        super();
        this.element = { url };
    }
    build() {
        return this.element;
    }
    addButton(button) {
        this.element.buttons = this.element.buttons || new Array();
        if (this.element.buttons.length === 3) {
            throw new Error("couldn't add next Button to Open Graph Element (only 3 buttons is allowed)");
        }
        this.element.buttons.push(button);
    }
}
exports.OgElementBuilder = OgElementBuilder;
