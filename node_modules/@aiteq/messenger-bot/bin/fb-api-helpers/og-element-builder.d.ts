import { Send } from "../fb-api";
import { ButtonHoldingBuilder } from "./button-holding-builder";
export declare class OgElementBuilder extends ButtonHoldingBuilder<Send.OpenGraphElement> {
    private element;
    constructor(url: string);
    build(): Send.OpenGraphElement;
    protected addButton(button: Send.Button): void;
}
