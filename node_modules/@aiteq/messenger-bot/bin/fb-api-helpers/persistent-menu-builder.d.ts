import { MessengerProfile } from "../fb-api";
import { Builder } from "./builder";
import { MenuBuilder } from "./menu-builder";
import { PersistentMenuDef } from "./persistent-menu-def";
export declare class PersistentMenuBuilder extends Builder<MessengerProfile.PersistentMenu[]> {
    private menuDef;
    static createMenu(): MenuBuilder;
    private static checkMenu(menu);
    private menus;
    constructor(menuDef?: PersistentMenuDef | PersistentMenuDef[]);
    build(): MessengerProfile.PersistentMenu[];
    addMenu(locale: string, composerInputDisabled: boolean, menu: MenuBuilder): this;
    private buildMenu(items);
}
