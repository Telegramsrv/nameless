import { Send } from "../fb-api";
import { Builder } from "./builder";
export declare abstract class MessageBuilder<T extends Send.Message> extends Builder<T> {
    protected message: T;
    addQuickReply(title: string, id: string, data?: string | any, imageUrl?: string): this;
    addLocationQuickReply(): this;
    build(): T;
}
