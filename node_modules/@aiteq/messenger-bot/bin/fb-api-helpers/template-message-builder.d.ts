import { Send } from "../fb-api";
import { MessageBuilder } from "./message-builder";
export declare abstract class TemplateMessageBuilder<T> extends MessageBuilder<Send.AttachmentMessage> {
    protected template: T;
    constructor();
    build(): Send.AttachmentMessage;
}
