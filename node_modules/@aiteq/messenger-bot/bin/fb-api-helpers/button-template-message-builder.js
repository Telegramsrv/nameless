"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const fb_api_1 = require("../fb-api");
const button_holding_template_message_builder_1 = require("./button-holding-template-message-builder");
class ButtonTemplateMessageBuilder extends button_holding_template_message_builder_1.ButtonHoldingTemplateMessageBuilder {
    constructor(text) {
        super();
        this.template = {
            template_type: fb_api_1.Send.TemplateType.BUTTON,
            text,
            buttons: new Array()
        };
    }
    addButton(button) {
        if (this.template.buttons.length === 3) {
            throw new Error("couldn't add next Button to Button Tepmlate message (only 1-3 buttons is allowed)");
        }
        this.template.buttons.push(button);
    }
}
exports.ButtonTemplateMessageBuilder = ButtonTemplateMessageBuilder;
