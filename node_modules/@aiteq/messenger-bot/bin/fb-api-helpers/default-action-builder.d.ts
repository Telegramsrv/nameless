import { Send, Webview } from "../fb-api";
import { Builder } from "./builder";
export declare class DefaultActionBuilder extends Builder<Send.DefaultAction> {
    private action;
    constructor(url: string);
    setWebviewHeightRatio(webviewHeightRatio: Webview.HeightRatio): this;
    setMessengerExtensions(messengerExtensions: boolean): this;
    setFallbackUrl(fallbackUrl: string): this;
    setShowWebviewShareButton(visible: boolean): this;
    build(): Send.DefaultAction;
}
