import { Send, Webview } from "../fb-api";
import { ChatExtension } from "../index";
import { ButtonHoldingBuilder } from "./button-holding-builder";
export declare class ElementBuilder extends ButtonHoldingBuilder<Send.Element> {
    private element;
    constructor(title: string);
    setSubtitle(subtitle: string): this;
    setImageUrl(imageUrl: string): this;
    setDefaultAction(url: string, options?: {
        webviewHeightRatio?: Webview.HeightRatio;
        messengerExtensions?: boolean;
        fallbackUrl?: string;
        webviewShareButton?: boolean;
    }): this;
    setExtensionDefaultAction(extension: ChatExtension, options?: {
        webviewHeightRatio?: Webview.HeightRatio;
        messengerExtensions?: boolean;
        fallbackUrl?: string;
        webviewShareButton?: boolean;
    }): this;
    build(): Send.Element;
    protected addButton(button: Send.Button): void;
}
