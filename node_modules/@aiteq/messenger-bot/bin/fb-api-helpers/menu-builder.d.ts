import { MessengerProfile, Webview } from "../fb-api";
import { Builder } from "./builder";
export declare class MenuBuilder extends Builder<MessengerProfile.MenuItem[]> {
    private actions;
    build(): MessengerProfile.MenuItem[];
    addWebUrlMenuItem(title: string, url: string, options?: {
        webviewHeightRatio?: Webview.HeightRatio;
        messengerExtensions?: boolean;
        shareButton?: boolean;
        fallbackUrl?: string;
    }): this;
    addPostbackMenuItem(title: string, id: string, options?: {
        data?: any;
        webviewHeightRatio?: Webview.HeightRatio;
        messengerExtensions?: boolean;
        shareButton?: boolean;
        fallbackUrl?: string;
    }): this;
    addSubmenu(title: string, submenu: MenuBuilder): this;
    private addMenuItem(item, options?);
}
