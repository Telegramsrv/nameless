import { BotUtils } from "../../utils/bot-utils";
import { Group } from "../group";
export declare class MessengerCodeGroup extends Group {
    constructor();
    execute(command: string, botUtils: BotUtils, options: any): Promise<string>;
    getUsage(): string;
}
