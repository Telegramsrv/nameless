import * as Graph from "./graph-api";
export declare class Api extends Graph.Api<Graph.Request> {
    protected accessToken: string;
    constructor(accessToken: string);
    getAppInfo(): Promise<Response>;
}
export interface Response {
    link: string;
    id: string;
    name: string;
    namespace: string;
}
