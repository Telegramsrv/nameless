import { AxiosRequestConfig } from "axios";
export declare abstract class Api<T extends Request> {
    protected accessToken: string;
    protected endpoint: Endpoint | string;
    private method;
    protected version: string;
    private static readonly DEFAULT_VERSION;
    private client;
    constructor(accessToken: string, endpoint?: Endpoint | string, method?: Method, version?: string);
    protected sendRequest(data: T, config?: AxiosRequestConfig): Promise<any>;
}
export declare enum Method {
    GET = "get",
    POST = "post",
    DELETE = "delete",
}
export declare enum Endpoint {
    MESSAGES = "me/messages",
    MESSENGER_PROFILE = "me/messenger_profile",
    MESSENGER_CODES = "me/messenger_codes",
    USER_PROFILE = "USER-PROFILE",
}
export interface Request {
    access_token?: string;
}
