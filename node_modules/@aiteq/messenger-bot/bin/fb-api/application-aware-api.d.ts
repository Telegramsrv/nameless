import { AxiosRequestConfig } from "axios";
import * as Graph from "./graph-api";
export declare class Api<T extends Graph.Request> extends Graph.Api<T> {
    protected pageAccessToken: string;
    protected appSecret: string;
    protected endpoint: string;
    private appInfo;
    private appId;
    constructor(pageAccessToken: string, appSecret: string, endpoint: string);
    protected sendRequest(data: T, config?: AxiosRequestConfig): Promise<any>;
}
