import * as ApplicationAware from "./application-aware-api";
import * as Graph from "./graph-api";
export declare class Api extends ApplicationAware.Api<Graph.Request> {
    protected pageAccessToken: string;
    protected appSecret: string;
    constructor(pageAccessToken: string, appSecret: string);
    getWebhookSubscriptions(): Promise<Subscription[]>;
    getSubscription(topic: SubscriptionTopic): Promise<Subscription>;
    getCallbackUrl(topic: SubscriptionTopic): Promise<string>;
}
export interface Response {
    data: Subscription[];
}
export interface Subscription {
    object: SubscriptionTopic;
    callback_url: string;
    fields: Field[];
    active: boolean;
}
export interface Field {
    name: string;
    version: string;
}
export declare enum SubscriptionTopic {
    PAGE = "page",
    USER = "user",
    PERMISSIONS = "permissions",
    PAYMENTS = "payments",
}
