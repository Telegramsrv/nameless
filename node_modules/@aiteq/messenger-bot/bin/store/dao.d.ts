export declare class Dao<T> {
    private path;
    private idProperty;
    private db;
    private table;
    constructor(path: string, idProperty?: string);
    get(id: any): T;
    save(entity: T): Promise<T>;
}
