import { Chat } from "./chat";
import { RouterService } from "./router-service";
export declare class ResponderService extends RouterService {
    private accessToken;
    private hearHandlers;
    private chats;
    private sendApi;
    private userProfileApi;
    constructor(accessToken: string);
    hear(hooks: RegExp[], handler: (chat: Chat, text: string, matches: string[]) => void): this;
    private processTextMessage(message, chat);
    private processAttachment(message, chat);
    private processPostback(postback, chat);
    private processQuickReply(message, chat);
    private processEcho(message, chat);
    private processDelivery(delivery, chat);
    private processRead(read, chat);
}
