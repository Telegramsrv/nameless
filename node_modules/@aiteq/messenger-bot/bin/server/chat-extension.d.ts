export declare abstract class ChatExtension {
    private view;
    private url;
    constructor(view: string);
    abstract getModel(): any;
    getView(): string;
    setBaseUrl(baseUrl: string): void;
    getUrl(): string;
}
