import { Webhook } from "../fb-api";
import { BotConfig } from "../utils/bot-config";
import { ChatExtension } from "./chat-extension";
import { EventHandler, HearHandler } from "./chat-service";
export declare class BotServer {
    private config;
    static normalizePort(value: number | string): number | string | boolean;
    private static readonly RE_ESCAPE;
    private static regExpEscape(text);
    private app;
    private server;
    private chatService;
    private extensions;
    private profileApi;
    private subscriptionsApi;
    private callbackUrl;
    constructor(config: BotConfig);
    start(): void;
    stop(): void;
    hear(hooks: RegExp | string | Array<RegExp | string>, callback: HearHandler): this;
    on(event: Webhook.Event, callback: EventHandler): this;
    on(event: Webhook.Event, id: string, callback: EventHandler): this;
    addChatExtension(extension: ChatExtension): this;
    private verifyRequest;
}
